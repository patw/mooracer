[package]
name = "mooracer-engine"
description = "MooRacer core: in-memory document data engine (value trees, indexes, queries, search, aggregation)"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true

[dependencies]

[lib]
name = "mooracer_engine"
path = "src/lib.rs"
